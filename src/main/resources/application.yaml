spring:
  application:
    name: movie-management-api
    profiles:
        active: local

    jpa:
      open-in-view: false
      hibernate:
        ddl-auto: validate

    flyway:
      enabled: true
      locations: classpath:db/migration